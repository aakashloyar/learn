The main packages are:

os → basic file operations
io → stream-based reading/writing
io/ioutil → older helpers (deprecated, use os and io now)
path/filepath → file paths and directory walking
bufio → buffered reading/writing
embed → embed files into binaries (very useful)

